## Task 1: Lab Setup

- Installed Parrot OS in VMware  
- Updated system: `sudo apt update && sudo apt upgrade -y`  
- Installed tools: `arpspoof`, `dsniff`, `wireshark`, `ettercap`, `driftnet`  
- Configured network for local MITM testing (no internet required)

>  Command to install all tools

  ```bash
    sudo apt install dsniff wireshark ettercap-graphical driftnet -y
  ```
